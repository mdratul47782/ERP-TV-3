// 🔹 Auth context (default: null)
import { createContext } from "react";

export const AuthContext = createContext(null);

// 🔹 Production auth context (default: null)
export const ProductionAuthContext = createContext(null);
